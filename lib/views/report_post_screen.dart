import 'package:flutter/material.dart';
import 'package:musix_admin/widget/appbar.dart';

class ReportPostScreen extends StatelessWidget {
  const ReportPostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CustomAppbar(title: "Report Post Manager"),
      body: SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
